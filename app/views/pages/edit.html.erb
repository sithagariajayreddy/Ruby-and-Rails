<%= error_messages_for(@page) %>
<div class="pages new">
<h2> Update page</h2>

  <%= form_for(:page, :url => {:action => 'update', :id => @page.id, :subject_id => @subject.id}) do |f| %>

  <table summary="Page form fields">
    <tr>
      <th><%= f.label(:subject_id)%></th>
      <td><%= f.select(:subject_id, @subjects.map{|s| [s.name, s.id]})%></td>
    </tr>
    <tr>
      <th><%= f.label(:name)%></th>
      <td><%= f.text_field(:name)%></td>
    </tr>
    <tr>
      <th><%= f.label(:permalink)%></th>
      <td><%= f.text_field(:permalink)%></td>
    </tr>
    <tr>
      <th><%= f.label(:position)%></th>
      <td><%= f.select(:position, 1..@page_count)%></td>
    </tr>
    <tr>
      <th><%= f.label(:visible)%></th>
      <td><%= f.check_box(:visible)%></td>
    </tr>
     <tr>
      <th><%= f.label(:created_at)%></th>
      <td><%= f.datetime_select(:created_at, :order => [:month, :day, :year])%></td>
    </tr>
  </table>

  <div class="form_buttons">
    <%= submit_tag("Update Page")%>
  </div>
  <%end%>
</div>

<%= link_to("Back To List", {:action => 'index', :subject_id => @subject.id}, :class => 'back to link')%>
